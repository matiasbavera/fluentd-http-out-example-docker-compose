``` bash
# To run all
docker-compose up -d 

# to run individually
docker-compose up -d simple-web-app
docker-compose up -d file-myapp
docker-compose up -d fluentd

# if you want to see the console output
docker-compose up  simple-web-app
```